# A tutorial for server side scripting

@ main.py
python -m venv vevn
Run : venv/scripts/activate for a python env
pip show fastapi

# Run server:

uvicorn main:app --reload

# We'll go through ci-cd and deployment (auto) operations
